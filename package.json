{
  "name": "onfile",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @onfile/app dev",
    "build": "pnpm --filter @onfile/app build",
    "preview": "pnpm --filter @onfile/app preview",
    "typecheck": "pnpm -r exec tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
